// Re-export the wire schema + marker grammar from the source-of-truth package
// so downstream consumers can import everything through this client SDK.
export * from "@tylerwarburton/sprite-core-schema";
